cd C:\Schoolwerk\Process-safety					Ga naar de map in Powershell

ls				kijk of je goed zit

docker run --rm -v "${PWD}:/book" -p 3000:3000 peaceiris/mdbook serve --hostname 0.0.0.0		RUn commando that er een webserver van maakt via docker

http://localhost:3000

Stop is ctrl+c netzo als in Linux		of weg klikken

